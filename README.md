# Calculate FLSM and VLSM easily

1. Activate virtual enviroment
2. Install required modules `pip install -r requirements.txt`
3. Run test units with `pytest`
4. Check for style suggestions with `style50 *.py`
